<Window x:Class="QuickPrompt.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="#1E1F22" Foreground="#F3F3F3">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Base URL"/>
        <TextBox Grid.Row="1" Text="{Binding BaseUrl}" Margin="0,4,0,8"/>

        <TextBlock Grid.Row="2" Text="API Key"/>
        <PasswordBox x:Name="ApiKeyBox" Grid.Row="3" Margin="0,4,0,8" PasswordChanged="ApiKeyBox_OnPasswordChanged"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,4,0,8">
            <StackPanel Width="220">
                <TextBlock Text="Default model"/>
                <TextBox Text="{Binding DefaultModel}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Width="120" Margin="12,0,0,0">
                <TextBlock Text="Temperature"/>
                <TextBox Text="{Binding Temperature}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Width="120" Margin="12,0,0,0">
                <TextBlock Text="Max tokens"/>
                <TextBox Text="{Binding MaxTokens}" Margin="0,4,0,0"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="5" Text="Additional headers (JSON object):"/>
        <TextBox Grid.Row="5" Margin="0,24,0,0" Height="140" Text="{Binding AdditionalHeadersJson}" AcceptsReturn="True"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="90" IsDefault="True"/>
            <Button Content="Cancel" Width="90" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
