<Window x:Class="QuickPrompt.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки"
        Width="620"
        Height="560"
        MinWidth="560"
        MinHeight="520"
        WindowStartupLocation="CenterOwner"
        Background="#0B1119"
        Foreground="#F4F8FD">
    <Window.Resources>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#A9BCD0" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#161E2A" />
            <Setter Property="Foreground" Value="#F4F8FD" />
            <Setter Property="BorderBrush" Value="#1FFFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#35C6D7" />
            <Setter Property="Foreground" Value="#071218" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#4CDBE7" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2FB2C4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#14FFFFFF" />
            <Setter Property="Foreground" Value="#F4F8FD" />
            <Setter Property="BorderBrush" Value="#1FFFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#22FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#30FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="18"
            Padding="24"
            Background="#101824"
            BorderBrush="#22FFFFFF"
            BorderThickness="1"
            CornerRadius="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Настройки подключения"
                           FontFamily="Segoe UI Variable Display, Segoe UI"
                           FontSize="28"
                           FontWeight="SemiBold" />
                <TextBlock Text="QuickPrompt использует эти параметры для списка моделей и отправки чата."
                           Margin="0,8,0,0"
                           Foreground="#9EB3C8"
                           FontSize="13" />
            </StackPanel>

            <ScrollViewer Grid.Row="1"
                          Margin="0,24,0,24"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Base URL" Style="{StaticResource FieldLabelStyle}" />
                    <TextBox Text="{Binding BaseUrl}" Style="{StaticResource FieldTextBoxStyle}" />

                    <TextBlock Text="API Key" Margin="0,20,0,8" Style="{StaticResource FieldLabelStyle}" />
                    <PasswordBox x:Name="ApiKeyBox"
                                 Padding="12,10"
                                 Background="#161E2A"
                                 Foreground="#F4F8FD"
                                 BorderBrush="#1FFFFFFF"
                                 BorderThickness="1"
                                 PasswordChanged="ApiKeyBox_OnPasswordChanged" />

                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                            <TextBlock Text="Default model" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding DefaultModel}" Style="{StaticResource FieldTextBoxStyle}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                            <TextBlock Text="Temperature" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding Temperature}" Style="{StaticResource FieldTextBoxStyle}" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Max tokens" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding MaxTokens}" Style="{StaticResource FieldTextBoxStyle}" />
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,20,0,0">
                        <TextBlock Text="Способ вызова assistant" Style="{StaticResource FieldLabelStyle}" />
                        <ComboBox ItemsSource="{Binding ActivationModes}"
                                  SelectedItem="{Binding ActivationMode}"
                                  Background="#161E2A"
                                  Foreground="#F4F8FD"
                                  BorderBrush="#1FFFFFFF"
                                  BorderThickness="1"
                                  Padding="12,8" />
                        <TextBlock Margin="0,8,0,0"
                                   FontSize="12"
                                   Foreground="#94A9BC"
                                   Text="Доступно: Ctrl+Shift+Space, DoubleShift, MiddleMouse." />
                    </StackPanel>

                    <TextBlock Text="Additional headers (JSON)"
                               Margin="0,20,0,8"
                               Style="{StaticResource FieldLabelStyle}" />
                    <TextBox Text="{Binding AdditionalHeadersJson}"
                             Height="170"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Style="{StaticResource FieldTextBoxStyle}" />
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Сохранить"
                        Width="130"
                        Margin="0,0,10,0"
                        Command="{Binding SaveCommand}"
                        IsDefault="True"
                        Style="{StaticResource PrimaryButtonStyle}" />
                <Button Content="Отмена"
                        Width="110"
                        IsCancel="True"
                        Style="{StaticResource GhostButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
